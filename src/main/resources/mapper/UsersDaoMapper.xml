<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa.springboot06.dao.UsersDao">
   <!-- <select id="listAll" resultType="map">
        select * from users u left join role r on u.rid=r.rid
    </select>
    <select id="listRole" resultType="map">
        select * from role
    </select>
    <select id="selectId" resultType="map" parameterType="int">
        select * from users where id=#{id}
    </select>
    <insert id="add" parameterType="Users">
        <selectKey keyProperty="id" resultType="Integer" order="BEFORE">
            select seq_test.nextval from dual
        </selectKey>
        insert into users values(#{id},#{name},#{password},#{rid})
    </insert>
    <update id="upd" parameterType="Users">
       update users set name=#{name},password=#{password},rid=#{rid} where id=#{id}
    </update>
    <delete id="delete" parameterType="int">
        delete from users where id=#{id}
    </delete>-->
</mapper>